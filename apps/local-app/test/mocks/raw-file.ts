/**
 * Mock for Vite's ?raw query string imports.
 * Jest doesn't understand ?raw imports, so we return an empty string.
 * This is used for markdown and other raw file imports in tests.
 */
export default '';
