/**
 * Re-export domain types from shared location.
 * Types are defined in @/ui/types/domain for use across all UI features.
 */
export type { Status, Epic, Agent, EpicsQueryData } from '@/ui/types/domain';
