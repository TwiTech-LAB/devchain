Local App (placeholder)

This folder hosts the Local App (NestJS + Vite SSR) codebase.
- Runs locally, manages tmux/pty sessions and the web UI.
- Uses StorageService in Local mode (SQLite) or Cloud mode (Remote API client).

See top-level README and docs/ for architecture and standards.

